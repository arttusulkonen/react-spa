import React from 'react';const Recipes = (props) => {    const {recipes} = props;  return (    <>      <div key={recipes.idMeal}>        <h1>{recipes.strMeal}</h1>        <img className={'recipe-image'} src={recipes.strMealThumb} alt={recipes.strMeal}/>        <h6>Category: {recipes.strCategory}</h6>        {recipes.strArea ? <h6>Area: {recipes.strArea}</h6> : null}        <p>{recipes.strInstructions}</p>      </div>      <div className={'recipe-content'}>        <table className={'striped'}>          <thead>          <tr>            <th>Ingredient</th>            <th>Measure</th>          </tr>          </thead>          <tbody>          {            Object.keys(recipes).map((key) => {              if(key.includes('Ingredient') && recipes[key]) {                return (                  <tr key={key}>                    <td>{recipes[key]}</td>                    <td>                      {                        recipes[`strMeasure${key.slice(13)}`]                      }                    </td>                  </tr>                )              }              return null;            })          }          </tbody>        </table>      </div>    </>  );};export default Recipes;